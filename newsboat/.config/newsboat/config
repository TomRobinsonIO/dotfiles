include ~/.config/newsboat/dark 

# browser "brave %u"

unbind-key j
unbind-key k
unbind-key h
unbind-key H
unbind-key L

# Bind keys
bind-key h quit
bind-key j down
bind-key k up
bind-key l open
bind-key H next-feed
bind-key L prev-feed
bind-key u pageup
bind-key d pagedown
